#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, const char* argv[]){
    int n = atoi(argv[1]);
    int *ptr = malloc(size_t sizeof(n));

    for (i=2; i<=n/2; ++i){
        if(n%i == 0){
            
        }
    }

}